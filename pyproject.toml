[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyLibmpsse"
version = "0.1.0"
description = "Python wrapper for libmpsse — FTDI MPSSE (SPI/I2C/JTAG) operations"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.12"
authors = [
    {name = "Nikolas.Young", email = "nikolas_young@foxmail.com"},
]
keywords = ["ftdi", "mpsse", "spi", "i2c", "jtag"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]

[project.urls]
Homepage = "https://github.com/liutao12632/pyLibmpsse"
Issues   = "https://github.com/liutao12632/pyLibmpsse/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
pyLibmpsse = ["*.dll"]  # 如果后续决定附带 DLL